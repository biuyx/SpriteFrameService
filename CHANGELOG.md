# 更新日志

## 2026-08-14

### 核心变化:从「会话」升级为「精灵」管理

- **精灵库首页**:启动不再是匿名会话,而是角色卡片墙——每个精灵(角色)管理自己的全部动作,进度一目了然
- **动作看板**:每个动作一张卡(封面缩略图 + 状态徽标 + 帧数/抠图/导出统计),支持定稿标记、常用动作名快捷创建(idle/walk/run/attack…)
- **工作台面包屑**:`精灵库 / 厨师 / walk`,随时返回上一层
- **数据永久保留**:关服务、重启,精灵和动作数据都在;旧会话可认领进精灵,也可直接删除

### AI 视频生成(Seedance)

- 动作内直接生成视频:**首帧参考图(必填)+ 提示词 → 生成 → 抽帧**,全流程不出工作台
- **三模型可选**:2.0 Fast(默认)/ 2.0 Mini(成本最低)/ 2.0 Pro;默认 480p / 4s
- **按动作名自动生成提示词模板**:建个叫 walk 的动作,提示词自动填好(绿幕背景、首帧锚定、禁特效、锁机位——按已验证的精灵图生产规则编写)
- **素材版本墙**:每次生成/上传是一个独立版本,可预览、对比、一键切换使用、删除(删付费生成的版本有提示)
- 生成中实时显示进度;服务重启后自动找回未完成的远端任务;常见报错(欠费/限流/审核)显示中文提示
- 并发上限可在设置中调整(默认 5),超出的任务自动排队

### 设置面板

- 界面内直接配置 **Ark 密钥**(打码显示、保存即生效、不入 git 不进安装包)和**生成并发数**

### 追溯与一致性

- **工序记录**:每个动作从素材到导出的完整参数链(抽帧参数、抠图模型、缩放比例…)自动记录,在「历史回退」页查看
- **以此帧建动作**:帧管理里选一帧,一键为同角色开新动作,首帧自动继承(角色形象连续)
- **工艺预设**:抠图参数全角色统一,偏离时明确警示,可一键还原或保存为预设

### 修复

- 确认类操作(生成/删除)在部分浏览器环境点击无反应 → 全部改为应用内确认框
- 生成完成后版本墙和视频预览不更新
- 切换素材版本后预览仍播旧视频
- 视频编码不支持时黑屏无提示 → 显示明确说明(不影响抽帧)
- 抠图参数陷阱内联提示(Alpha 阈值 1–50 反而更糟、羽化加重白边)
- 魔棒编辑与批量任务的并发冲突

### 安装包

- 新增 **无模型精简包**(约 86 MB,`-SkipModels` 构建):不装模型也能跑通「生成 → 抽帧 → 绿幕颜色抠图 → 导出」完整链路;AI 抠图/姿势分析/超分按说明书下载模型
- 使用生成功能需在「⚙ 设置」中填入火山方舟 API 密钥

## 2026-08-13

- 精灵帧服务基线:风险修复(数据持久化/内存/安全)、访问认证、内网访问、Windows 免安装绿色包、抠图/姿势/超分模型接入
