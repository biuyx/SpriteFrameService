# 更新日志

## 2026-09-15

### Spine 资源导出(新)

- **一键出 Spine 资源**:动作看板「导出 Spine」,把角色的全部序列帧打成骨架 JSON + 图集(`.atlas`/`.png`)+ `images/`。美术在 Spine 里 **Import Data** 选该 JSON 即可打开为工程,另存为 `.spine`
- **动画名统一走模板库**:参考视频库新增「Spine 动画名」列可逐条编辑,也可按变体名一键套用建议值(走路→walk1、工作→sit_work1…);导出前查重名并拦下
- **导出模板:导入既有工程,照抄它的约定**。填参考工程目录(或 `.json`/`.skel`)即可反解出骨架版本、帧命名格式与起始帧号、渲染尺寸与画布、图集压缩比,以及每个动画的插槽名/骨骼偏移/缩放/帧率。这些只存在于工程文件里,不导入就复现不出来
  - 自带 Spine 3.8 二进制骨架解析;其他版本请用 Spine 导出的 `.json` 作参考
  - 参考工程里如有真骨骼动画、多套皮肤、网格形变,导入时直接报错拒绝,不会悄悄导出一个错的
- **产物常驻列表**:已导出的产物随时可再下载或删除,手动导的与流水线收口导的都在一处

### 工序编排调整

- **缩放与描边改到导出环节**,不再就地改帧。此前两者都写回处理图层、覆盖抠图结果(描边开自动扩边还会改帧尺寸),且与导出环节的尺寸处理叠乘——角色在 128 画布里的占比会从 61% 一路掉到 26%。现在帧文件全程保持原分辨率
- **执行顺序固定为 缩放 → 描边**:先定稿尺寸再描边,描边宽度填几 px,成品里就是几 px
- **按比例缩放改为每帧按自身尺寸算**。原来用首帧算出目标尺寸再套给所有帧,遇到帧尺寸不齐的动作会把它们强行拉成一样大
- **参数存成精灵预设**:在「图像处理」两个面板里各点一次「存为导出预设」,普通导出、流水线导出、Spine 导出三条路共用同一份
- **流水线新增角色级收口**:勾「完成后导出 Spine 资源」,这批动作全部跑完后自动打包整个角色。同时跑完也只导一次,跨重启有效;中途有失败的就不导,补跑完成后自动接上
- 一键流水线的计划表里会列出导出时将应用的缩放与描边

### 角色复制与描边

- **新建精灵可选已有角色作模板**:继承动作骨架与模板绑定,可选带上首帧与参考图
- **完整复制角色**:连同素材版本、帧、抠图结果、导出一起复制,用于 A/B 抽卡对比。复制时会改写帧数据里的绝对路径,副本不会指回源目录
- **图像处理新增描边**:纯色描边,可设宽度、颜色、位置(外/内/居中)与风格(平滑/硬边/方角硬边),抠图完成后执行,支持单帧预览
  - 旧版描边只写 RGB 不写 alpha,外侧半宽永远透明,等于没描;现改用 alpha 距离变换重写

## 2026-09-09

- **自动流水线**:首帧 → 视频生成 → 抽帧 → 抠图 → 导出 一键执行,执行前有计划预览(每步 执行/跳过/阻塞及原因),首帧生成后可暂停等人工确认

## 2026-09-08

- 工作台拆分素材工序:首帧 / 视频生成 / 抽帧 三步独立
- 提示词库(多版本 / 绑定匹配动作 / 动作记忆)、首帧图库总览、BRIA 默认抠图
- 资产管线增强:列表化 UI、参考库分组、首帧生成、抽帧规则、项目包导入导出
- 首帧可直接设为精灵正面/背面立绘(入图库并标记,同朝向排他)
- 导航体验优化:hash 路由、动作切换器、流水线左栏、顶栏资源库、看板多选与直达

## 2026-08-20

- 批量导入:动作模板库 + 角色×动作矩阵建档 + 提示词分流
- 批量生成:勾选队列 + 进度面板 + 失败重试
- 参考媒体改经 OSS 中转(Ark 拒绝 base64 内嵌的参考视频);OSS 配置进设置面板

## 2026-08-14

### 核心变化:从「会话」升级为「精灵」管理

- **精灵库首页**:启动不再是匿名会话,而是角色卡片墙——每个精灵(角色)管理自己的全部动作,进度一目了然
- **动作看板**:每个动作一张卡(封面缩略图 + 状态徽标 + 帧数/抠图/导出统计),支持定稿标记、常用动作名快捷创建(idle/walk/run/attack…)
- **工作台面包屑**:`精灵库 / 厨师 / walk`,随时返回上一层
- **数据永久保留**:关服务、重启,精灵和动作数据都在;旧会话可认领进精灵,也可直接删除

### AI 视频生成(Seedance)

- 动作内直接生成视频:**首帧参考图(必填)+ 提示词 → 生成 → 抽帧**,全流程不出工作台
- **三模型可选**:2.0 Fast(默认)/ 2.0 Mini(成本最低)/ 2.0 Pro;默认 480p / 4s
- **按动作名自动生成提示词模板**:建个叫 walk 的动作,提示词自动填好(绿幕背景、首帧锚定、禁特效、锁机位——按已验证的精灵图生产规则编写)
- **素材版本墙**:每次生成/上传是一个独立版本,可预览、对比、一键切换使用、删除(删付费生成的版本有提示)
- 生成中实时显示进度;服务重启后自动找回未完成的远端任务;常见报错(欠费/限流/审核)显示中文提示
- 并发上限可在设置中调整(默认 5),超出的任务自动排队

### 设置面板

- 界面内直接配置 **Ark 密钥**(打码显示、保存即生效、不入 git 不进安装包)和**生成并发数**

### 追溯与一致性

- **工序记录**:每个动作从素材到导出的完整参数链(抽帧参数、抠图模型、缩放比例…)自动记录,在「历史回退」页查看
- **以此帧建动作**:帧管理里选一帧,一键为同角色开新动作,首帧自动继承(角色形象连续)
- **工艺预设**:抠图参数全角色统一,偏离时明确警示,可一键还原或保存为预设

### 修复

- 确认类操作(生成/删除)在部分浏览器环境点击无反应 → 全部改为应用内确认框
- 生成完成后版本墙和视频预览不更新
- 切换素材版本后预览仍播旧视频
- 视频编码不支持时黑屏无提示 → 显示明确说明(不影响抽帧)
- 抠图参数陷阱内联提示(Alpha 阈值 1–50 反而更糟、羽化加重白边)
- 魔棒编辑与批量任务的并发冲突

### 安装包

- 新增 **无模型精简包**(约 86 MB,`-SkipModels` 构建):不装模型也能跑通「生成 → 抽帧 → 绿幕颜色抠图 → 导出」完整链路;AI 抠图/姿势分析/超分按说明书下载模型
- 使用生成功能需在「⚙ 设置」中填入火山方舟 API 密钥

## 2026-08-13

- 精灵帧服务基线:风险修复(数据持久化/内存/安全)、访问认证、内网访问、Windows 免安装绿色包、抠图/姿势/超分模型接入
